# Чек-лист проекта Vedic AI (Rag Agent)

Этот документ содержит обзор текущего состояния разработки, выделяя реализованный функционал и задачи, которые еще предстоит выполнить.

## 📱 Фронтенд (React Native)

### ✅ Реализовано
- [x] **Интерфейс чата**: Полнофункциональный UI для общения с AI.
- [x] **Рендеринг Markdown**: Поддержка форматированного текста, списков и ссылок.
- [x] **Работа с изображениями**: Отображение картинок от AI, возможность скачивания и шаринга (`fileService.ts`).
- [x] **Регистрация пользователя**: 
    - [x] Двухфазная форма (Email/Pass + Полный профиль).
    - [x] Выбор страны/города с автозаполнением столиц.
    - [x] Сохранение данных в `UserContext` и `AsyncStorage`.
    - [x] Интеграция с навигацией (переход в Portal после завершения).
- [x] **Техническая база (Fixes)**:
    - [x] Исправлена ошибка `Native part of Reanimated not initialized`.
    - [x] Устранен конфликт `ViewManagerWithGeneratedInterface` (downgrade Gesture Handler & Screens).
    - [x] Стабилизирован React Navigation 6 (снижен с v7 для совместимости).
    - [x] Централизованный `API_PATH` и поддержка переменных окружения через `.env`.
- [x] **Настройки моделей**: Выбор текстовых, аудио и графических моделей через `SettingsDrawer`.
- [x] **Инфраструктура i18n**: Настройка многоязычности (русский/английский).
- [x] **Система чат-комнат (Rooms)**:
    - [x] Создание комнат с уникальными именами и аватарами.
    - [x] Управление участниками (приглашение друзей, удаление, назначение админов).
    - [x] **Настройки комнаты**: Переключение публичности и активация ИИ-помощника через меню `⋮`.
    - [x] **ИИ-Помощник в комнатах**: Интеграция AI, который видит контекст переписки и участвует в диалоге.
    - [x] **Саммари (Краткое содержание) чата**: Генерация краткого отчета о последних обсуждениях в комнате.
- [x] **Локальное хранение**: Сохранение профиля и настроек через `AsyncStorage`.
- [x] **Интеграция с прокси-сервером**: Все запросы к AI идут через Go-бекенд.
- [x] **Работа с изображениями**: Отображение картинок от AI, возможность скачивания и шаринга (`fileService.ts`).
- [x] **Сервис знакомств (Vedamatch)**:
    - [x] **Профиль знакомств**: Расширенные поля (био, интересы, астро-данные).
    - [x] **Удобный ввод**: Нативный выбор времени (`DatePicker`) и полноэкранный поиск городов через Nominatim API.
    - [x] **Медиабиблиотека**: Загрузка нескольких фото, управление альбомом, установка фото профиля.
    - [x] **AI Совместимость**: Анализ совместимости кандидатов с помощью ИИ на основе духовных и астрологических данных.
    - [x] **Поиск кандидатов**: Отображение реальных пользователей из базы данных.

### ⏳ В процессе / Ожидает реализации (Mocked)
- [x] **Real-time Messaging (WebSockets)**: Внедрен WebSocket для мгновенной доставки сообщений без обновления. Реализован Hub на бэкенде и WebSocketContext на фронтенде. (<time>2025-12-25</time>)
- [ ] **Порталы (Portal Screens)**: Разделы "Контакты", "Знакомства", "Магазины", "Объявления"  "Новости" и "База знаний" используют статичные **MOCK_DATA**. Нужна интеграция с реальными API.
     - [x] **Контакты**: Базовая интеграция с БД (отображение реальных пользователей).
          - [ ] **Статус Online**: Реальное отслеживание активности вместо случайной генерации.
          - [x] **Аватары**: Загрузка и отображение реальных фотографий профиля (Интеграция с S3 Timeweb).
          - [ ] **Взаимодействие**: Открытие профиля или переход в чат при клике на контакт.
          - [ ] **Система друзей**: Логика "Добавить в друзья" и фильтрация списка.
     - [x] **Знакомства (Vedamatch)**: 
          - [x] Поиск по духовным предпочтениям и Мадхве.
          - [x] Проверка совместимости через AI.
          - [x] Управление фотографиями.
          - [ ] Система лайков и уведомлений о взаимности (Matches).
     - [ ] **Магазины**: 
          - [ ] Каталог товаров (веган-продукты, атрибутика).
          - [ ] Корзина и интеграция с оплатой.
     - [ ] **Объявления**: 
          - [ ] Категории (жилье, услуги, вещи).
          - [ ] Геолокация объявлений.
     - [ ] **Новости**: 
          - [ ] Лента событий сообщества.
          - [ ] Push-уведомления о важных анонсах.
     - [ ] **База знаний**: 
          - [ ] Поиск по книгам и лекциям.
          - [ ] Интеграция с RAG для ответов на философские вопросы.

- [ ] **AI Помощник (Кришна)**:
     - [ ] **3D Взаимодействие**: Анимации реакции на сообщения (кивки, жесты).
     - [ ] **Голосовое управление**: Возможность диктовать сообщения и слушать ответы.
     - [ ] **Эмоциональный интеллект**: Смена выражения лица/ауры в зависимости от контекста беседы.

- [ ] **Синхронизация профиля**: Автоматический вход и получение данных профиля с сервера при запуске (сейчас только локально).
- [ ] **Валидация форм**: Более строгая проверка вводимых данных при регистрации.
- [ ] **История чата**: Сохранение и загрузка истории диалогов с сервера (сейчас хранится только в контексте сессии).

---

## ⚙️ Сервер (Go Backend)

### ✅ Реализовано
- [x] **API Framework**: Основа на Fiber v2 с настроенным роутингом.
- [x] **Прокси-чат**: Эндпоинт `/api/v1/chat/completions`, пересылающий запросы к внешнему AI провайдеру.
- [x] **Регистрация в БД**: Сохранение расширенных профилей пользователей в PostgreSQL через GORM.
- [x] **Интеграция с RAG (Gemini)**:
    - [x] Конвертация профиля в текстовый формат.
    - [x] Загрузка файлов (UserProfile) в Google Gemini API.
    - [x] Импорт файлов в RAG Corpus (Store).
    - [x] Сохранение `RagFileID` в базе данных.
- [x] **Docker Infrastructure**: Настроенные `Dockerfile` и `docker-compose.yml` для БД и сервера.

### ⏳ В процессе / Ожидает реализации
- [ ] **Персонализация чата (RAG Context)**: Сервер пока не извлекает данные из RAG Store при запросах в чат. Нужно внедрить поиск по Corpus перед отправкой запроса к AI.
- [ ] **Регистрация (Доработки)**:
    - [ ] Загрузка реального аватара на сервер (сейчас только выбор в UI).
    - [ ] Валидация уникальности Email на лету.
    - [ ] Подтверждение почты (OTP/Link).
- [ ] **Безопасность**:
    - [ ] Внедрение JWT-токенов для аутентификации запросов.
    - [ ] Защита эндпоинтов чата.
    - [ ] Настройка CORS и Rate limiting.
- [ ] **База данных для Порталов**: Создание таблиц и API для функционала Знакомств, Магазинов и т.д.
- [ ] **Управление Store ID**: Сделать `Corpus ID` динамическим или автоматически создаваемым при первом запуске.
- [ ] **Логирование и Мониторинг**: Внедрение структурированных логов для отслеживания ошибок Gemini API.

---

## 📊 Общий статус
- **Core (Чат/Регистрация)**: ~90%
- **RAG & AI Rooms**: ~80%
- **Portal Services (Знакомства/Медиа)**: ~40%
- **Security/DevOps**: ~30%

---

## 🚀 Ближайшие приоритеты (Roadmap)

1.  **🌟 Персонализация чата (RAG Retrieval)** — *Высокий приоритет*
    - Реализовать извлечение данных из RAG Store перед запросом к AI.
    - Настроить системный промпт для учета данных профиля (имя, диета, локация).

2.  **🛡️ Безопасность и JWT** — *Критично для VPS*
    - Внедрение авторизации по токенам для защиты API.
    - Сохранение сессии на фронтенде.

3.  **🖼️ Загрузка медиа (Аватары)** — *UX улучшение*
    - Создание эндпоинта для загрузки файлов на бэкенд.
    - Хранение путей к изображениям в БД.

4.  **💬 Реальное время (WebSockets)** — *Критично для чатов*
    - Замена HTTP-запросов на WebSockets для мгновенного обмена сообщениями.
    - Улучшение UI (индикаторы "печатает...", статусы доставки).

5.  **🎭 Роли ИИ в комнатах** — *Функциональное расширение*
    - Возможность выбирать "личность" или роль ИИ для каждой комнаты (Наставник, Критик, Секретарь).

---

## 📍 Улучшение локации и API

### ✅ Реализовано
- [x] **Автоматическое кеширование**: Данные о странах и городах сохраняются локально (`locationService.ts`).
- [x] **Fallback списки**: Встроенные списки стран и городов для работы без интернета.
- [x] **Опциональный ручной ввод**: Возможность вводить город вручную, если API не находит его.
- [x] **Предзагрузка**: Загрузка списка стран при старте приложения для ускорения UX.
- [x] **GeoLocation API**: Реализован сервис `geoLocationService.ts` для определения местоположения по GPS и IP.

### ⏳ Будущие улучшения (To-Do)
- [x] **UI для очистки кеша**: Добавить кнопку в настройки для обновления данных о городах.
- [x]  **Унификация сервисов**: Объединить `useLocation.ts` и `locationService.ts` для устранения дублирования кода.
- [ ] **Полный офлайн режим**: Расширение локальных списков для полноценной работы без доступа к сети.
- [ ] **История локаций**: Запись и быстрый выбор предыдущих местоположений пользователя.
- [ ] **Поиск по регионам**: Добавить поддержку штатов, провинций и регионов при выборе места.
- [ ] **Авто-определение при регистрации**: Автоматическое заполнение полей города и страны через GPS при первом запуске.


## 🚀 Рекомендации: что еще можно сделать

### 1. Улучшение UX и взаимодействия
*   [X] **Индикаторы набора текста:** Показывать "Имя пользователя печатает...", используя WebSocket события. (нужно протестировать)
*   [X] **Статусы сообщений:** Внедрить галочки: одна (отправлено), две (доставлено), синие (прочитано). (нужно протестировать)
*   [x] **Медиа-сообщения (в разработке):** Добавить возможность отправлять фотографии, голосовые сообщения и документы прямо в чат.
     - [x] S3 Storage интеграция (Timeweb Cloud) ✅
     - [ ] **Backend:**
        - [ ] Обновить модель Message для поддержки типов: `text`, `image`, `audio`, `document`
        - [ ] Создать эндпоинт `POST /api/messages/media` для загрузки файлов в S3
        - [ ] Поддержка multipart/form-data с файлами и метаданными
        - [ ] Обновить messageService для отправки медиа сообщений через WebSocket
        - [ ] Валидация типов файлов и ограничение размера (максимум 10MB для фото, 20MB для документов)
     - [ ] **Frontend:**
        - [ ] Добавить react-native-image-picker для выбора фото из галереи
        - [ ] Добавить react-native-document-picker для выбора документов
        - [ ] Добавить react-native-audio-recorder-player для записи голосовых сообщений
        - [ ] Обновить ChatInput: добавить кнопки для фото/аудио/документов
        - [ ] Обновить MessageList: рендеринг изображений, аудио плеера, превью документов
        - [ ] Обновить ChatContext: поддержка отправки медиа сообщений
        - [ ] Прогресс бар загрузки файлов
        - [ ] Кэширование загруженных файлов локально
     - [ ] **UI/UX:**
        - [ ] Предпросмотр фото перед отправкой
        - [ ] Визуализация длительности аудио сообщений
        - [ ] Иконки разных типов файлов (PDF, DOC, etc)
        - [ ] Возможность скачивания документов
     - [ ] **Тестирование:**
        - [ ] Тестирование загрузки файлов разного формата
        - [ ] Тестирование S3 fallback на локальное хранилище
        - [ ] Тестирование WebSocket доставки медиа сообщений
*   [ ] **Реакции на сообщения:** Быстрые ответы эмодзи (лайки, поклоны и т.д.) на конкретные сообщения.
*   [ ] **Ответы (Replies):** Возможность отвечдавать на конкретное сообщение (цитирование).

### 2. Продвинутый ИИ функционал
*   [ ] **Личности ИИ (AI Roles):** Дать возможность выбирать "личность" для ИИ в комнате (например, "Эксперт по Шримад Бхагаватам", "Организатор мероприятий" или "Философский критик").
*   [ ] **Проактивность ИИ:** Настроить ИИ так, чтобы он мог сам начинать диалог, если в комнате долго никто не писал, предлагая тему для обсуждения или стих дня.
*   [ ] **Поиск по истории с ИИ:** Возможность спросить ИИ: "О чем мы договорились в прошлый вторник в этом чате?", и он найдет ответ в базе данных.

### 3. Сообщество и поиск
*   [ ] **Глобальный поиск преданных:** Поиск не только по городу, но и по интересам, духовному учителю или стилю йоги.
*   [ ] **Статусы пользователей:** Текстовые статусы (например, "Читаю джапу", "В храме", "Доступен для общения").
*   [ ] **Упоминания (Mentions):** Поддержка `@username` для уведомления конкретного пользователя в групповом чате.

### 4. Технические доработки
*   [ ] **Push-уведомления:** Интеграция с Firebase (FCM) для уведомлений о новых сообщениях, когда приложение закрыто.
*   [ ] **Оффлайн-режим:** Кэширование сообщений в локальную базу данных (SQLite/WatermelonDB), чтобы чаты можно было читать без интернета.
*   [ ] **Безопасность (JWT):** Полное покрытие всех эндпоинтов авторизацией по токенам для защиты переписки.

---

## 📸 План реализации медиа-сообщений

### Обзор
Добавить поддержку отправки фото, голосовых сообщений и документов в чат, используя S3 Storage (Timeweb Cloud) для хранения файлов.

### Архитектура решения

```
Frontend (React Native)
    ├── Выбор файла (ImagePicker/DocumentPicker)
    ├── Запись аудио (AudioRecorder)
    ├── Загрузка файла на backend
    │      POST /api/messages/media (multipart/form-data)
    │         ├─ file: бинарные данные
    │         ├─ type: 'image' | 'audio' | 'document'
    │         ├─ senderId: uint
    │         ├─ recipientId?: uint
    │         └─ roomId?: uint
    ↓
Backend (Go)
    ├── Валидация файла (тип, размер)
    ├── Загрузка в S3 (s3_service.go)
    ├── Создание записи в БД (Message)
    │   └─ Type: 'image'/'audio'/'document'
    │   └─ Content: S3 URL файла
    └─ Broadcast через WebSocket
```

### Этапы реализации

#### Этап 1: Backend - Подготовка инфраструктуры

**Задачи:**

1. **Обновить модель Message** (`server/internal/models/message.go`)
   - Добавить поля для хранения метаданных файла:
   ```go
   type Message struct {
       // ... существующие поля ...
       Type        string `json:"type" gorm:"default:'text'"` // 'text', 'image', 'audio', 'document'
       Content     string `json:"content"` // Текст или URL файла
       FileName    string `json:"fileName,omitempty"` // Название файла
       FileSize    int64  `json:"fileSize,omitempty"` // Размер в байтах
       MimeType    string `json:"mimeType,omitempty"` // MIME тип
       Duration    int    `json:"duration,omitempty"` // Длительность аудио (сек)
   }
   ```

2. **Создать handler для загрузки медиа** (`server/internal/handlers/media_handler.go`)
   ```go
   func (h *MediaHandler) UploadMessageMedia(c *fiber.Ctx) error
   ```
   - Принимает multipart/form-data
   - Валидация: фото (до 10MB, .jpg/.png), аудио (до 5MB, .mp3/.m4a), документы (до 20MB, .pdf/.doc/.docx/.txt)
   - Загружает в S3 с ключом: `messages/{type}/{userId}_{timestamp}.{ext}`
   - Создает запись Message в БД
   - Broadcast через WebSocket

3. **Добавить маршрутизацию** (`server/cmd/api/main.go`)
   ```go
   app.Post("/api/messages/media", mediaHandler.UploadMessageMedia)
   ```

4. **Валидация типов файлов**
   ```go
   var allowedTypes = map[string][]string{
       "image":    {"image/jpeg", "image/png", "image/gif"},
       "audio":    {"audio/mpeg", "audio/mp4", "audio/wav"},
       "document": {"application/pdf", "application/msword", "text/plain"},
   }
   ```

#### Этап 2: Frontend - Выбор и отправка файлов

**Задачи:**

1. **Установить зависимости**
   ```bash
   cd frontend
   pnpm add react-native-image-picker react-native-document-picker react-native-audio-recorder-player @react-native-clipboard/clipboard @react-native-fs/app-fs
   ```

2. **Создать сервис для работы с медиа** (`frontend/services/mediaService.ts`)
   ```typescript
   export interface MediaFile {
       uri: string;
       type: string;
       name: string;
       size: number;
       mimeType?: string;
   }

   export const mediaService = {
       pickImage: (): Promise<MediaFile> => { ... }
       pickDocument: (): Promise<MediaFile> => { ... }
       recordAudio: (): Promise<MediaFile> => { ... }
       uploadMedia: (media: MediaFile, recipientId?: number, roomId?: string): Promise<Message> => { ... }
   }
   ```

3. **Обновить ChatInput** (`frontend/components/chat/ChatInput.tsx`)
   - Добавить кнопки для медиа:
   - 📷 Фото (справа от input)
   - 🎙️ Голос (зажатие)
   - 📎 Документ (в меню)

   ```tsx
   <View style={styles.mediaButtons}>
       <TouchableOpacity onPress={handlePickImage}>
           <Icon name="camera" />
       </TouchableOpacity>
       <TouchableOpacity onPress={handlePickDocument}>
           <Icon name="attach" />
       </TouchableOpacity>
       <TouchableOpacity onPressIn={handleStartRecording} onPressOut={handleStopRecording}>
           <Icon name="mic" />
       </TouchableOpacity>
   </View>
   ```

4. **Обновить ChatContext** (`frontend/context/ChatContext.tsx`)
   - Добавить функции:
   ```typescript
   handleSendMedia: (media: MediaFile) => Promise<void>
   handleSendVoice: (audioUri: string) => Promise<void>
   ```

5. **Создать компонент AudioRecorder** (`frontend/components/chat/AudioRecorder.tsx`)
   - UI для записи голоса (кнопка, таймер, визуализация)
   - Остановить запись при отпускании кнопки

6. **Создать компонент DocumentPreview** (`frontend/components/chat/DocumentPreview.tsx`)
   - Иконка типа файла
   - Название и размер
   - Кнопка скачать/открыть

#### Этап 3: Frontend - Отображение медиа сообщений

**Задачи:**

1. **Обновить MessageList** (`frontend/components/chat/MessageList.tsx`)
   - Рендеринг в зависимости от `message.type`:
   ```tsx
   const renderMessageContent = (message: Message) => {
       switch (message.type) {
           case 'image':
               return <Image source={{ uri: message.content }} style={styles.messageImage} />
           case 'audio':
               return <AudioPlayer url={message.content} duration={message.duration} />
           case 'document':
               return <DocumentPreview fileName={message.fileName} url={message.content} size={message.fileSize} />
           default:
               return <Markdown>{message.content}</Markdown>
       }
   }
   ```

2. **Создать AudioPlayer** (`frontend/components/chat/AudioPlayer.tsx`)
   - Управление воспроизведением (play/pause)
   - Прогресс бар
   - Отображение длительности
   - Можно использовать существующий WebView подход

3. **Обновить Message тип** (`frontend/components/chat/ChatConstants.ts`)
   ```typescript
   export interface Message {
       id: string;
       text: string;
       sender: 'user' | 'bot' | 'other';
       type?: 'text' | 'image' | 'audio' | 'document';
       fileName?: string;
       fileSize?: number;
       duration?: number;
       createdAt?: Date;
       navTab?: any;
   }
   ```

#### Этап 4: UI/UX улучшения

**Задачи:**

1. **Предпросмотр фото**
   - Показать выбранное фото перед отправкой
   - Возможность отменить

2. **Индикатор записи аудио**
   - Анимация записи
   - Таймер (мм:сс)
   - Визуализация звуковой волны

3. **Иконки типов файлов**
   - PDF: 📄
   - DOC: 📝
   - TXT: 📃
   - Другие: 📎

4. **Прогресс загрузки**
   - Показывать при отправке файла
   - Возможность отменить загрузку

5. **Кэширование изображений**
   - Использовать `react-native-fast-image` для быстрой загрузки
   - Локальное кэширование для офлайн просмотра

#### Этап 5: WebSocket интеграция

**Задачи:**

1. **Broadcast медиа сообщений** (`server/internal/websocket/hub.go`)
   - При загрузке медиа на backend, сразу отправлять через WebSocket
   - Включает все метаданные (URL, тип, размер и т.д.)

2. **Обработка на фронтенде** (`frontend/context/WebSocketContext.tsx`)
   - При получении медиа сообщения, обновлять список сообщений
   - Оптимизировать загрузку больших файлов (lazy loading)

3. **Индикатор доставки**
   - Показывать загрузку файла для отправителя
   - Показывать галочку ✅ после получения recipient-ом

#### Этап 6: Тестирование

**Сценарии тестирования:**

1. **Фотографии**
   - ✅ Выбор из галереи
   - ✅ Загрузка на сервер
   - ✅ Отображение в чате
   - ✅ Zoom/просмотр полного размера
   - ✅ Скачать/сохранить
   - ✅ Поддержка PNG, JPG, GIF

2. **Голосовые сообщения**
   - ✅ Начало записи (нажатие)
   - ✅ Остановка записи (отпускание)
   - ✅ Загрузка аудио
   - ✅ Воспроизведение в чате
   - ✅ Pause/Resume
   - ✅ Прогресс бар

3. **Документы**
   - ✅ Выбор из файлов
   - ✅ Загрузка на сервер
   - ✅ Отображение в чате
   - ✅ Скачать/открыть
   - ✅ Поддержка PDF, DOC, DOCX, TXT

4. **Пограничные случаи**
   - ✅ Отмена загрузки
   - ✅ Ошибка сети
   - ✅ Недопустимый тип файла
   - ✅ Превышение размера файла
   - ✅ S3 недоступен (fallback на local storage)

5. **WebSocket**
   - ✅ Мгновенная доставка медиа сообщений
   - ✅ Синхронизация между устройствами
   - ✅ Обработка повторной отправки (дедупликация)

### Технические требования

#### Требования к файлам

| Тип | Форматы | Макс. размер | Превью |
|-----|---------|--------------|--------|
| Фото | JPG, PNG, GIF | 10MB | Thumbnail (150x150) |
| Аудио | MP3, M4A, WAV | 5MB | Длительность, размер |
| Документы | PDF, DOC, DOCX, TXT | 20MB | Иконка типа, размер |

#### Конфигурация S3

- **Бакет messages/** - для чат файлов
- **Структура:** `messages/{type}/{userId}_{timestamp}.{ext}`
- **Public access:** Только чтение (GET)
- **TTL:** Хранить неактивные файлы 30 дней (lifecycle policy)

#### Метрики мониторинга

- Среднее время загрузки файла
- Успешность загрузок (%)
- Объем хранилища по типам
- Активность WebSocket сообщений

### Ожидаемый результат

После реализации пользователи смогут:
- 📷 Отправлять фотографии из галереи в чат
- 🎙️ Записывать голосовые сообщения нажатием кнопки
- 📎 Прикреплять документы (PDF, DOC, TXT)
- 📥 Скачивать и просматривать медиа файлы
- 🔄 Получать медиа сообщения мгновенно через WebSocket
- 📱 Просматривать медиа в офлайн режиме (кешированные изображения)

### Следующие шаги после реализации

1. **Реакции на сообщения** - эмодзи реакции на медиа
2. **Ответы на сообщения** - цитирование конкретного сообщения (включая медиа)
3. **Редактирование сообщений** - возможность редактировать текстовые сообщения
4. **Удаление сообщений** - удаление как для себя, так и "для всех"
5. **Пересылка сообщений** - переслать медиа сообщение в другой чат