# Environment for USB Device (with adb reverse)
# adb reverse redirects localhost:8081 from device to host machine
# API_OPEN_AI is now managed by the backend proxy
API_BASE_URL=http://localhost:8081
APP_ENV=development
