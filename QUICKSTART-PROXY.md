# Быстрый старт: Исправление ошибки "Missing required field: provider"

## Проблема
Roo Code Nightly не передает параметр `provider` в запросах к API, что вызывает ошибку 400.

## Решение за 3 шага

### Шаг 1: Установите зависимости
```bash
cd /home/maxim/Documents/Rag-agent
pnpm install
```

### Шаг 2: Запустите прокси-сервер
```bash
npm start
# или
node proxy-server.js
```

Вы увидите:
```
🚀 Прокси-сервер запущен на порту 3001
📡 Перенаправляет запросы на: https://rvlautoai.ru/webhook
🔧 Провайдер по умолчанию: Capi
```

### Шаг 3: Настройте Roo Code Nightly

1. Откройте настройки Cursor (Ctrl+, или Cmd+,)
2. Найдите настройки плагина "Roo Code Nightly"
3. Найдите поле "Base URL" или "API Base URL"
4. Измените на: `http://localhost:3001`
5. Сохраните и перезапустите Cursor

## Изменение провайдера

Если нужно использовать другой провайдер (например, HuggingSpace):

```bash
DEFAULT_PROVIDER=HuggingSpace node proxy-server.js
```

## Проверка работы

После настройки попробуйте использовать плагин. В консоли прокси-сервера вы увидите логи:
```
[PROXY] Добавляю параметр provider: Capi
[PROXY] Модель: claude45sonnetthinking, Провайдер: Capi
```

Если ошибка исчезла - всё работает! ✅

## Остановка прокси-сервера

Нажмите `Ctrl+C` в терминале, где запущен прокси-сервер.

