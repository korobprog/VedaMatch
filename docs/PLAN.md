# План реагирования на инцидент безопасности: Утечка учетных данных

## 🔴 КРИТИЧЕСКАЯ ТРЕВОГА
**Контекст**: Файл `.env`, содержащий производственные секреты (ключи Google Gemini, S3, TURN, JWT, пароли Superadmin), был закоммичен в репозиторий (строки 1-28).
**Серьезность**: КРИТИЧЕСКАЯ. Требуются немедленные действия.

## Обзор
Этот план описывает шаги по изоляции, устранению последствий и проверке инцидента безопасности. Цель — удалить конфиденциальный файл из истории git и сменить (ротировать) все скомпрометированные учетные данные для предотвращения несанкционированного доступа.

## Тип проекта
**БЕЗОПАСНОСТЬ / DEVOPS**

## Критерии успеха
1. Файл `.env` удален из ветки `master` и добавлен в `.gitignore`.
2. Файл `.env` полностью удален из всей истории git (все ветки/теги).
3. Все скомпрометированные секреты (Gemini, S3, TURN, JWT, rvlautoai) заменены на новые.
4. Учетные данные супер-админа изменены.
5. Приложение успешно перезапускается и работает с новыми учетными данными.
6. Сканирование безопасности проходит без обнаружения критических секретов.

## Технологический стек
-   **Git**: `git filter-branch` или `bfg` (для перезаписи истории)
-   **Скрипты**: Python (для сканирования безопасности)
-   **Сервисы**: AWS, Google Cloud, Custom Backend

## Разбивка задач

### Фаза 1: Изоляция и очистка истории (DevOps)
| ID задачи | Название | Агент | Навыки | Приоритет | Входные данные | Выходные данные | Проверка |
|-----------|----------|-------|--------|-----------|----------------|-----------------|----------|
| 1.1 | **Удалить .env локально и добавить в игнор** | `devops-engineer` | `bash-linux` | **P0** | Корень репозитория | Обновленный `.gitignore`, `.env` удален из git | `git status` показывает `.env` как игнорируемый |
| 1.2 | **Очистить историю Git** | `devops-engineer` | `bash-linux` | **P0** | История Git | Очищенная история | `git log --all -- .env` ничего не возвращает |
| 1.3 | **Принудительный пуш (Force Push) чистой истории** | `devops-engineer` | `bash-linux` | **P0** | Очищенный локальный репо | Обновленный удаленный репо | `git show` на удаленном хеше не показывает `.env` |

### Фаза 2: Ротация ключей (Backend & Security)
| ID задачи | Название | Агент | Навыки | Приоритет | Входные данные | Выходные данные | Проверка |
|-----------|----------|-------|--------|-----------|----------------|-----------------|----------|
| 2.1 | **Сменить API ключ Google Gemini** | `backend-specialist` | `api-patterns` | **P0** | Старый ключ | Новый ключ | Тестовый вызов API с новым ключом |
| 2.2 | **Сменить ключи AWS S3** | `backend-specialist` | `server-management` | **P0** | Старые Access/Secret | Новые Access/Secret | Тест загрузки/скачивания S3 |
| 2.3 | **Сменить секрет TURN** | `backend-specialist` | `server-management` | **P0** | Старый секрет | Новый секрет | Проверка WebRTC соединения |
| 2.4 | **Сменить JWT_SECRET и инвалидировать токены** | `security-auditor` | `vulnerability-scanner` | **P0** | Старый JWT_SECRET | Новый JWT_SECRET | Старые токены не проходят валидацию |
| 2.5 | **Сбросить пароль супер-админа** | `security-auditor` | `database-design` | **P0** | Старый пароль | Новый пароль в БД | Вход с новым паролем работает |
| 2.6 | **Сменить ключи rvlautoai** | `backend-specialist` | `api-patterns` | **P0** | Старый ключ | Новый ключ | Тест интеграции сервиса |

### Фаза 3: Восстановление окружения (Backend)
| ID задачи | Название | Агент | Навыки | Приоритет | Входные данные | Выходные данные | Проверка |
|-----------|----------|-------|--------|-----------|----------------|-----------------|----------|
| 3.1 | **Создать безопасный .env.example** | `backend-specialist` | `documentation-templates` | **P1** | Содержимое `.env` | `.env.example` (очищенный) | В `.env.example` нет реальных значений |
| 3.2 | **Обновить локальное окружение** | `backend-specialist` | `bash-linux` | **P1** | Новые ключи | Локальный `.env` обновлен | Приложение запускается локально |

### Фаза 4: Верификация (Security & Plan Mode)
| ID задачи | Название | Агент | Навыки | Приоритет | Входные данные | Выходные данные | Проверка |
|-----------|----------|-------|--------|-----------|----------------|-----------------|----------|
| 4.1 | **Запустить сканирование безопасности** | `security-auditor` | `vulnerability-scanner` | **P0** | Кодовой база | Отчет сканирования | `security_scan.py` проходит успешно |
| 4.2 | **Полная E2E проверка системы** | `test-engineer` | `webapp-testing` | **P1** | Запущенное приложение | Результаты тестов | Ключевые сценарии (Вход, Загрузка, Чат) работают |

## ✅ ФАЗА X ЗАВЕРШЕНА
- [ ] Линтер: Пройден
- [ ] Безопасность: Нет критических проблем (Ручная проверка очистки истории)
- [ ] Сборка: Успешно
- [ ] Дата: Будет определена
