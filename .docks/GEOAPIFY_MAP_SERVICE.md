# 🗺️ Сервис Интерактивной Карты (Geoapify)

## 💡 Основная концепция
Создание единой интерактивной гео-экосистемы приложения, которая объединяет социальное взаимодействие, коммерцию и частный сектор на одной визуальной панели. Сервис позволяет пользователям не только видеть охват сообщества по всему миру, но и мгновенно взаимодействовать с объектами инфраструктуры и другими участниками.

### Ключевые возможности:
*   **Глобальный охват сообщества**: Визуализация плотности пользователей по городам с помощью системы умных кластеров.
*   **Интерактивная коммерция**: Отображение магазинов, кафе и ведических центров с возможностью мгновенного перехода в карточку магазина и совершения покупок.
*   **Гео-мессенджер**: Инициация диалога с пользователем прямо через карту (интеграция с существующей системой чатов).
*   **Единая доска объявлений**: Поиск услуг, жилья и мероприятий по географическому признаку с использованием продвинутых фильтров.
*   **Масштабируемость и кастомные слои**: Архитектура позволяет легко интегрировать новые специализированные сервисы отображения (например, **"Лучшие места для санкиртаны"**, ведические школы или паломнические маршруты).
*   **Интеллектуальная фильтрация**: Возможность гибко настраивать отображение слоев (только магазины, только люди или всё вместе).

---

## 1. Архитектура данных (Backend - Go)

Для эффективной работы карты мы создаем специализированный слой агрегации данных, чтобы не перегружать мобильное приложение.

### Эндпоинты (Handlers)
*   `GET /api/map/summary` — Возвращает статистику по городам для кластеризации на мелких масштабах.
    *   **Данные:** `[{ city: string, userCount: int, shopCount: int, lat: float, lng: float }]`
*   `GET /api/map/markers` — Возвращает детальные объекты для текущей области видимости пользователя (Bounding Box).
    *   **Параметры:** `lat_min`, `lat_max`, `lng_min`, `lng_max`, `categories[]`.

### Логика объединения
Бекенд собирает данные из трех основных таблиц:
1.  `users` — фильтрует тех, у кого `latitude` и `longitude` не null.
2.  `shops` — берет активные магазины и их категории (кафе, аюрведа и т.д.).
3.  `ads` — выбирает актуальные объявления, привязанные к локациям.

---

## 2. Интеграция Geoapify

### Настройка (Security)
*   **API Key:** Хранится исключительно в `server/.env` под переменной `GEOAPIFY_API_KEY`.
*   **Proxying:** Все запросы к Geoapify (кроме загрузки тайлов карты) проходят через наш Go-сервер для защиты ключа и кеширования результатов.

### Стилизация
Использование кастомного стиля Tiles API, соответствующего эстетике "Vedic AI":
*   Цветовая схема: Мягкие природные тона (Terracotta, Sage, Cream).
*   Отключение лишних городских элементов для фокуса на маркерах сообщества.

---

## 3. Фронтенд и Пользовательский Интерфейс (React Native)

### Основные компоненты
*   `MapGeoapifyScreen.tsx` — Основной экран карты.
*   `MapMarkerCustom.tsx` — Кастомный маркер, меняющий вид в зависимости от типа объекта.

### Типы маркеров и действия
| Объект | Иконка (Пример) | Действие при нажатии |
| :--- | :--- | :--- |
| **Город (Кластер)** | Круг с числом | Зум в город или переход в общий чат региона |
| **Пользователь** | Аватар / 👤 | Открытие профиля + Кнопка "Написать" (Messenger) |
| **Магазин/Кафе** | 🍎 / 🛍️ | Переход в существующий сервис магазинов (`ShopDetails`) |
| **Объявление** | 📋 / 🏷️ | Открытие карточки объявления (`AdDetails`) |

### Фильтрация (UI Layers)
Плавающее меню (Fab-buttons) для включения/выключения слоев:
*   `[X] Сообщество (Люди)`
*   `[X] Коммерция (Магазины/Услуги)`
*   `[X] Частные объявления`

---

## 4. Сценарии взаимодействия (Deep Linking)

1.  **Чат из карты:** Если пользователь нажимает на маркер другого человека, система проверяет наличие `roomId` через `messengerService`. Если комнаты нет — создает ее и открывает `ChatScreen`.
2.  **Экосистема Магазинов:** При клике на кафе на карте открывается экран магазина, где уже реализована корзина и список товаров.
3.  **Маршруты:** Добавление кнопки "Маршрут" в карточку объекта, использующей Geoapify Routing API для построения пути от текущей геопозиции.

---

## 5. Технический стек
*   **Backend:** Go (Fiber), GORM (PostgreSQL/SQLite).
*   **Frontend:** React Native, `react-native-maps`, Geoapify Tile Service.
*   **API:** Geoapify (Places, Routing, Static/Dynamic Maps).
