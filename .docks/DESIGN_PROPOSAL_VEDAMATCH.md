# Предложение по редизайну VedaMatch AI

## 1. Концепция: "Modern Vedic Ethereal" (Современная Ведическая Эстетика)
Текущий дизайн функционален, но использует слишком яркие, базовые цвета и системные компоненты (эмодзи, стандартные шрифты). Новая концепция должна объединить духовность с премиальной технологичностью.

**Ключевая метафора:** "Свет знания сквозь призму современности". Интерфейс должен быть легким, "воздушным", с использованием эффектов стекла (Glassmorphism), мягкого свечения и сакральной геометрии.

---

## 2. Цветовая Палитра (Design Tokens)

Мы уходим от "кислотного" оранжевого к более глубоким и благородным оттенкам.

### Основные цвета (Primary)
- **Saffron Deep**: `#E65100` (Глубокий шафрановый) — для основных кнопок и акцентов.
- **Marigold Gold**: `#FFB300` (Золото бархатцев) — для градиентов и выделения важного.

### Вторичные цвета (Secondary)
- **Vrindavan Peacock**: `#004D40` (Глубокий изумрудно-бирюзовый) — альтернативный акцент для контраста.
- **Lotus Pink**: `#D81B60` (Лотосовый) — для уведомлений и лайков.

### Фоны (Backgrounds)
- **Day Mode**: `#FFF8E1` (Мягкий кремовый/сливочный) — вместо чистого белого, более приятен глазу.
- **Night Mode**: `#121212` с оттенком `#1A237E` (Космический синий) — глубокий ночной фон.

---

## 3. Типографика (Typography)

Системные шрифты заменяются на пару с "характером".

- **Заголовки**: `Playfair Display` или `Cinzel` — элегантные шрифты с засечками, подчеркивающие традицию.
- **Основной текст**: `Nunito` или `Lato` — округлые, дружелюбные гротески для интерфейса.

---

## 4. UI Компоненты и Улучшения

### A. Навигация (PortalMainScreen)
**Проблема:** Горизонтальная прокрутка с эмодзи выглядит устаревшей и неудобной.
**Решение:**
1. **Bottom Tab Bar**: Перенести навигацию вниз. Использовать фиксированную панель с 4-5 основными разделами.
2. **Векторные иконки**: Заменить эмодзи (`👥`, `💬`) на качественные SVG иконки (библиотека `lucide-react-native` или `react-native-vector-icons`).
    - *Пример:* Иконка лотоса для "Знаний", два силуэта для "Знакомств".

### B. Карточки и Списки
**Проблема:** Жесткие тени и плоские цвета.
**Решение:**
1. **Glassmorphism**: Полупрозрачные подложки с размытием фона (`blur`).
2. **Мягкие границы**: Тонкие обводки (`1px solid rgba(255,255,255,0.2)`) вместо жирных теней.
3. **Card Gap**: Увеличить воздушное пространство между элементами.

### C. Экран входа (LoginScreen)
**Проблема:** Текстовые поля выглядят слишком стандартно на фоне яркого градиента.
**Решение:**
1. **Background**: Добавить тонкий паттерн "Мандала" или "Цветок жизни" с низкой прозрачностью (5-10%) поверх градиента.
2. **Inputs**: Поля ввода сделать полупрозрачными темными/светлыми (в зависимости от темы) с размытием фона.

---

## 5. План реализации для Stitch

Для разработчика (Stitch) переданы следующие инструкции:

1.  **Настройка темы**:
    -   Обновить `constants/colors.ts` новой палитрой.
    -   Подключить библиотеку иконок (`pnpm add react-native-vector-icons`).
    -   Загрузить шрифты (если необходимо, через `react-native-asset`).

2.  **Рефакторинг `LoginScreen`**:
    -   Применить новые градиенты.
    -   Заменить `TextInput` на кастомный компонент `GlassInput`.

3.  **Рефакторинг `PortalMainScreen`**:
    -   Заменить верхний `ScrollView` с табами на нижний `BottomTabBar` (можно использовать стандартный от React Navigation или кастомный).
    -   Убрать эмодзи, вставить иконки Feather/MaterialCommunity.

4.  **Стилизация**:
    -   Использовать `StyleSheet` или `styled-components` для создания переиспользуемых "стеклянных" контейнеров.

---

## 6. Визуализация (Описание)

*Представьте экран:*
Верхняя часть экрана (Header) чистая, без лишних кнопок, только аккуратный аватар и название раздела, написанное шрифтом *Cinzel*.
Фон имеет легкую текстуру бумаги или шума.
Внизу экрана — "парящая" навигационная панель (не прибитая к краю, а с отступами), полупрозрачная, с золотистой подсветкой активного элемента.
Иконки тонкие, изящные. Кнопка "AI" — это пульсирующий круг в центре нижней панели (или плавающая кнопка FAB), окрашенная в градиент Сангвины и Золота.
